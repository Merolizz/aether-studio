#pragma once

// Backward compatibility: ProjectContext is now an alias for GlobalProjectContext
#include "GlobalProjectContext.h"

// ProjectState enum is now in GlobalProjectContext.h
// All functionality is available through GlobalProjectContext or ProjectContext alias
