#pragma once

#include <QWidget>

namespace aether {

class DeliverPageWidget : public QWidget {
    Q_OBJECT
public:
    explicit DeliverPageWidget(QWidget* parent = nullptr);
};

} // namespace aether
